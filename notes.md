# Shortcuts for vs-code editor
generate react component
> imrc
-------------
generate class component 
> cc
-------------
key could be {tag.id}
<ul>
  {this.state.tags.map(tag => <li key={tag}>{tag}</li>)}
</ul>